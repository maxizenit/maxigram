# maxigram v2 — модульный монолит (Kotlin + Spring Boot 4.1 + Spring Modulith + jOOQ)

## Overview

Переработка проекта maxigram (соцсеть, изначально — бакалаврская ВКР 2024 г.) из набора
из 4 микросервисов (`user`/`chat`/`feed` + `api-service`-шлюз на gRPC, RabbitMQ, Keycloak,
Jmix/Vaadin-фронт) в **модульный монолит** на Kotlin/Spring Boot с переписанным фронтендом.

Защита ВКР не планируется — единственный критерий качества здесь инженерный:
читаемость, предсказуемость, отсутствие классов багов v1, поддерживаемость.

**Что меняется по сравнению с v1:**
- 4 микросервиса + gRPC + RabbitMQ → один монолит с модулями (Spring Modulith) и доменными событиями;
- Java → **Kotlin** (null-safety, `data class` для событий/DTO, лаконичность);
- JPA/Hibernate → **jOOQ** + Flyway (типобезопасный SQL, без cascade/lazy/N+1-магии);
- внешний Keycloak → встроенный **Spring Authorization Server** (свой модуль `identity`, всё в монолите);
- `api-service` без авторизации (identity из query-параметра) → **OAuth2 Resource Server**, identity из JWT (`sub`);
- Jmix/Vaadin без real-time → **React SPA** + REST + **WebSocket/STOMP**;
- добавляется модуль **уведомлений** (in-app + email);
- пакеты-копипаста `ru.maxizenit.backend.chatservice` → единый `org.maxizenit.maxigram` (reverse-DNS домена maxizenit.org).

**Что переносим из v1 (идеи, доведённые до рабочего состояния):**
профили/интересы/подписки; лента (посты, лайки, комментарии); обычные и анонимные чаты
со взаимной деанонимизацией; подбор собеседника по «похожести»; самоограничения (цифровое благополучие).

## Технологический стек (актуально на июнь 2026, версии совместимы между собой)

| Компонент | Версия | Примечание |
|---|---|---|
| Kotlin | 2.3 | Управляется Spring Boot 4.1 BOM (baseline 2.2) |
| Java (toolchain) | 25 (LTS) | First-class в Boot 4.1; jOOQ 3.20 baseline JDK 21 — совместимо |
| Spring Boot | 4.1.0 | На Spring Framework 7.0.8+ |
| Spring Modulith | 2.0.x | Ветка под Boot 4 / Framework 7 |
| Spring Authorization Server | под Boot 4.1 | Встроенный OAuth2/OIDC сервер |
| jOOQ | 3.20 | Поддержка Kotlin 2; кодоген из схемы |
| Flyway | из Boot 4.1 BOM | SQL-миграции — источник правды для jOOQ |
| PostgreSQL | 16+ | Единственная БД |
| Frontend | React + Vite + TypeScript | OIDC (Auth Code + PKCE), REST, STOMP |

## Context (из анализа v1)

**Реализация v1, перенесённая «как идея», но с дефектами:**
- `backend/chat-service` — чаты, анонимность, деанонимизация (логика сломана);
- `backend/feed-service` — посты/лайки/комментарии (лайки/комменты не персистятся);
- `backend/user-service` — профили/подписки/интересы + расчёт похожести (формула неверна, состояние in-memory);
- `backend/api-service` — REST-шлюз (нет проверки JWT);
- `frontend/jmix-client` — Vaadin UI (нет real-time).

**Карта дефектов v1 → как закрывается в v2** (подробности — в разделе Technical Details):

| # | Дефект v1 | Где | Решение v2 |
|---|-----------|-----|------------|
| 1 | identity из query-параметра, шлюз не проверяет токен | `api-service` | OAuth2 Resource Server, `sub` из JWT |
| 2 | self-restraint и часть gRPC — заглушки `super.*` | `UserServiceGrpcImpl` | Полная реализация модуля `wellbeing` |
| 3 | деанонимизация: 2-й участник ставит `false`; `newChatId = getNewChatId()` | `ChatService#agreeToDeAnonymization` | Исправленная доменная логика + тесты |
| 4 | лайки/комменты/подписки не сохраняются (владеющая сторона без cascade) | `PostService`/`CommentService`/`UserProfileService` | Явные INSERT/DELETE через jOOQ |
| 5 | ошибка в формуле похожести интересов (Infinity) | `SimilarityCalculationService` | Корректные формулы + защита от деления на ноль |
| 6 | in-memory очередь подбора и список расчётов (race, не масштабируются) | `AnonymousChatQueue`, `similarityCalculations` | Состояние в БД + доменные события Modulith |
| 7 | «последнее сообщение» = первое (`OrderById` без `Desc`) | `MessageService` | Запрос по `created_at DESC` |
| 8 | нет проверки участия в `getChatById` (IDOR) | `ChatServiceGrpcImpl` | Авторизация на уровне сервиса по `sub` |
| 9 | N+1 на счётчиках лайков/комментов | `FeedServiceGrpcImpl` | Агрегатные запросы jOOQ (`COUNT`, `EXISTS`) |
| 10 | нет real-time в чате; инверсия кнопки отправки | `MessagesView` | WebSocket/STOMP + корректный фронт |
| 11 | `Date.getYear()`, возраст без месяца/дня | `calculateAge` | `LocalDate` + `Period` |
| 12 | секреты в гите (`.env`), устаревший TLS, битый локальный `datasource.url` | инфраструктура | Профили + secrets вне VCS, актуальный TLS |

## Solution Overview

### Архитектурный стиль
Модульный монолит. Один деплой-артефакт, внутри — изолированные модули с явными границами,
проверяемыми **Spring Modulith** (`ApplicationModules.verify()` в тесте). Межмодульная связь —
только через публичные API модулей (named interfaces) и **доменные события**; прямой доступ к
внутренним пакетам и таблицам чужого модуля запрещён. Это сохраняет возможность вынести любой
модуль в отдельный сервис позже, без оверхеда сети/брокера сейчас.

### Модули
- `common` — общий kernel: типы-значения (`UserId`), утилиты, базовая конфигурация, обработка ошибок.
- `identity` — **Spring Authorization Server**: собственное хранилище пользователей, регистрация,
  пароли (Argon2), выдача OIDC-токенов, верификация email и сброс пароля. Также `CurrentUser` (resource side).
- `profile` — `UserProfile`, `Interest`, `Subscription`.
- `feed` — `Post`, `Comment`, `Like`, лента, счётчики.
- `chat` — `Chat`, `Message`, обычные/анонимные чаты, деанонимизация, WebSocket.
- `notification` — in-app уведомления (push через STOMP) + email; слушает доменные события других модулей.
- `matching` — подбор собеседника по похожести; слушает события, публикует результат.
- `wellbeing` — `SelfRestraint` (самоограничения).

### Доступ к данным
**jOOQ** как единственный слой доступа. Схема описывается миграциями **Flyway**
(`src/main/resources/db/migration`) — это источник правды. Классы jOOQ генерируются кодогеном
из применённой к временной БД схемы (Testcontainers/локальная БД на этапе сборки). Никаких сущностей
Hibernate, `cascade`, `mappedBy`, lazy-проксей и dirty-checking — только явные запросы. Каждый
модуль владеет своими таблицами; обращение к таблицам другого модуля — через его API.

### Безопасность и identity
`identity`-модуль поднимает **Spring Authorization Server** (OAuth2/OIDC): свой реестр клиентов,
выдача токенов, собственная модель пользователя (`app_user`), регистрация и флоу email-верификации/сброса
пароля. Остальное приложение — **OAuth2 Resource Server**: identity берётся исключительно из токена
(`JwtAuthenticationToken` → `sub` = `app_user.id`), а не из тела/параметров запроса. Все доменные
операции авторизуются по текущему пользователю.

### Уведомления
Модуль `notification` подписывается на доменные события Modulith (новое сообщение, подбор в анонимный
чат, новый подписчик, лайк/комментарий, а также события auth-флоу) и доставляет их двумя каналами:
**in-app** (push подписчику через STOMP) и **email** (Spring Mail). Email-инфраструктура переиспользуется
модулем `identity` для верификации/сброса пароля.

### Real-time
WebSocket поверх STOMP: подписка на чат (`/topic/chats/{chatId}`) и на личные уведомления
(`/user/queue/notifications`). Аутентификация WebSocket — тем же JWT при handshake.

### Фронтенд
React SPA (Vite + TypeScript), общение по REST, real-time — по WebSocket/STOMP. Аутентификация
через OIDC (Authorization Code + PKCE) к встроенному Authorization Server.

### Базовый пакет
Единый `org.maxizenit.maxigram` (reverse-DNS реального домена maxizenit.org). Конец разнобою
`ru.maxizenit` / `ru.maxigram` и копипасте `…backend.chatservice`.

## Technical Details

### Модель данных (Flyway-миграции, jOOQ-кодоген поверх)
Ориентировочные таблицы (точные типы/индексы фиксируются в задачах соответствующих модулей):
- `identity`: `app_user(id pk uuid, email unique, password_hash, email_verified, created_at)`;
  `email_verification_token`, `password_reset_token`; стандартные таблицы Spring Authorization Server
  (`oauth2_registered_client`, `oauth2_authorization`, `oauth2_authorization_consent`).
- `profile`: `user_profile(id pk → app_user.id)`, `first_name`, `last_name`, `birthdate`;
  `interest(id pk, name)`; `user_interest(user_id, interest_id)`;
  `subscription(subscriber_id, author_id)` — pk из двух колонок.
- `feed`: `post(id pk, author_id, text, created_at)`; `comment(id pk, post_id, author_id, text, created_at)`;
  `post_like(post_id, author_id)` и `comment_like(comment_id, author_id)` — раздельные таблицы лайков
  (в v1 одна таблица `like_` с двумя nullable-FK — заменяем на две явные).
- `chat`: `chat(id pk, first_participant_id, second_participant_id, anonymous, first_agreed, second_agreed,
  closed, new_chat_id)`; `message(id pk, chat_id, sender_id, text, created_at, read)`.
- `notification`: `notification(id pk, recipient_id, type, payload, read, created_at)`.
- `matching`: `match_request(user_id pk, created_at)`; `similarity_calc(id pk, first_user_id,
  second_user_id, age_sim, interests_sim, feed_sim, status)` — состояние расчёта в БД, не in-memory.
- `wellbeing`: `self_restraint(user_id pk, start_time, end_time)` — **с полями времени** (в v1 их не было).

### Ключевые потоки

**Идентификация.** `identity` выдаёт OIDC-токены, где `sub = app_user.id`. Каждый защищённый запрос
несёт JWT; `CurrentUser.id()` берётся из `sub`. Контроллеры не принимают `requesterId`/`authorId` от
клиента — только из контекста.

**Деанонимизация (исправление дефекта #3).** При согласии участника выставляется флаг **его** стороны
в `true` (в v1 для второго участника ставился `false`). Когда оба флага `true` — находится/создаётся
обычный чат, и в анонимный чат записывается `new_chat_id = <id нового чата>` (в v1 ошибочно брался
`getNewChatId()` нового чата, всегда `null`). Покрывается unit-тестами на обе стороны и идемпотентность.

**Подбор по похожести (исправление дефектов #5, #6).**
- Очередь кандидатов и состояние расчётов хранятся в БД (`match_request`, `similarity_calc`), а не в памяти
  инстанса — нет race condition и потери при рестарте.
- Вместо RabbitMQ — доменные события Spring Modulith: `matching` публикует запрос на сбор активности,
  `feed` отвечает событием, `matching` агрегирует и публикует результат; `chat` слушает результат и
  создаёт анонимный чат при превышении порога; `notification` уведомляет участников.
- Формула похожести интересов исправляется на симметричную:
  `(common/firstCount + common/secondCount) / 2` (в v1 второе слагаемое было `secondCount/common` → `Infinity`).
  Все коэффициенты защищаются от деления на ноль (проверка `count == 0`, а не только `isNaN`).
- Возраст — через `LocalDate`/`Period` с учётом месяца и дня (в v1 — deprecated `Date.getYear()`).

**Лента со счётчиками (исправление дефекта #9).** `likesCount`, `commentsCount`, `isLikedByCurrentUser`
считаются агрегатными запросами jOOQ (`COUNT`, `EXISTS`) одним-двумя запросами на страницу ленты, без
загрузки коллекций и N+1.

**Real-time сообщения (исправление дефектов #7, #10).** «Последнее сообщение» берётся запросом по
`created_at DESC LIMIT 1`. Новое сообщение рассылается подписчикам чата по WebSocket; фронт не опрашивает
по клику. Состояния кнопок (отправка/закрытие/деанонимизация) выводятся из состояния чата корректно.

## Development Approach
- **Подход к тестам: TDD** (тест → реализация → рефакторинг) для всей доменной логики.
- Завершать каждую задачу полностью до перехода к следующей; маленькие сфокусированные изменения.
- **Каждая задача обязана включать новые/обновлённые тесты** для своего кода (success + ошибочные/граничные сценарии).
- **Все тесты должны проходить перед началом следующей задачи** — без исключений.
- **Обновлять этот файл при изменении объёма работ.**

## Testing Strategy
- **Unit-тесты** — обязательны в каждой задаче (доменная логика: деанонимизация, формулы похожести, авторизация).
- **Интеграционные тесты** — на слой jOOQ и REST через **Testcontainers** (реальный PostgreSQL), на WebSocket-потоки.
- **Архитектурный тест** — `ApplicationModules.verify()` (Spring Modulith): границы модулей не нарушены.
- **e2e (фронт)** — Playwright по ключевым сценариям (логин, лента, отправка сообщения, согласие на деанонимизацию).

## Progress Tracking
- Отмечать выполненное `[x]` сразу по факту.
- Новые задачи помечать префиксом ➕, блокеры — ⚠️.
- Держать план в синхроне с реальной работой.

## What Goes Where
- **Implementation Steps** (`[ ]`): то, что делается в этом репозитории — код, миграции, тесты, документация.
- **Post-Completion** (без чекбоксов): внешние действия — деплой, ручная проверка, нагрузочное тестирование.

## Implementation Steps

> Структура: каждая задача — один логический модуль/эндпоинт/поток, с блоком **Files:** и тестами
> отдельными пунктами. TDD: тесты пишутся первыми. Точные имена jOOQ-классов появляются после кодогена.
> MVP-ядро = Этапы 0–4 (каркас, auth, профили, лента, чаты).

### Этап 0 — Каркас

#### Task 0.1: Инициализация Kotlin-монолита и сборки
**Files:**
- Create: `build.gradle.kts`, `settings.gradle.kts`
- Create: `src/main/kotlin/org/maxizenit/maxigram/MaxigramApplication.kt`
- Create: `src/main/resources/application.yaml`, `application-local.yaml`

- [x] создать Spring Boot 4.1 проект на Kotlin 2.3 (Java 25 toolchain), пакет `org.maxizenit.maxigram`
- [x] базовые зависимости: web, actuator, modulith-core, jackson-kotlin, kotlin-reflect, test, modulith-test
  - ➕ остальные стартеры (security, authorization-server, resource-server, jooq, flyway, mail, websocket, testcontainers) переехали в задачи, где впервые используются — иначе контекст 0.1 не поднимется без datasource
- [x] профили (`local`), секреты вне VCS (`.env` в `.gitignore`, `.env.example` в репо)
- [x] smoke-тест: контекст поднимается (`@SpringBootTest` → `contextLoads`)
- [x] прогнать тесты — `./gradlew test` BUILD SUCCESSFUL (Gradle 9.6.0, Kotlin 2.3.21, Java 25)

#### Task 0.2: Flyway + jOOQ-кодоген + Testcontainers ✅
**Files:**
- Create: `src/main/resources/db/migration/V1__baseline.sql`
- Modify: `build.gradle.kts` (конфигурация jOOQ codegen)
- Create: `src/test/kotlin/org/maxizenit/maxigram/support/AbstractIntegrationTest.kt`, `DatabaseLayerTest.kt`

- [x] настроить Flyway и применение миграций на старте (`spring-boot-starter-flyway` + `flyway-database-postgresql`)
- [x] настроить кодоген jOOQ из схемы через `DDLDatabase` (парсит SQL-миграции, без живой БД), генерация в `build/generated/jooq`
- [x] базовый интеграционный тест на Testcontainers: миграции применяются, jOOQ DSLContext работает
- [x] прогнать тесты — `./gradlew clean build` BUILD SUCCESSFUL, 3 теста зелёные

Замечания по факту реализации:
- jOOQ по факту **3.21.5** (а не 3.20) — версия из Boot 4.1 BOM; плагин кодогена и `jooq-meta-extensions` выровнены на неё.
- Testcontainers — **2.0.5** (Boot 4.1 BOM): модули переименованы (`testcontainers-junit-jupiter`, `testcontainers-postgresql`).
- В Spring Boot 4 автоконфигурации модульные — для Flyway нужен `spring-boot-starter-flyway`, голого `flyway-core` мало.
- ⚠️ Контейнер в тестах — **singleton** (стартует один раз, не по классу): иначе кэш контекста Spring указывает на остановленный контейнер.
- `CREATE EXTENSION` в baseline обёрнут в `-- [jooq ignore start/stop]` (Flyway исполняет, парсер кодогена пропускает).
- Кодоген пока генерит пустую схему (таблиц ещё нет); первые реальные jOOQ-классы появятся в Task 1.1.

#### Task 0.3: Объявление модулей и контроль границ (Spring Modulith)
**Files:**
- Create: `package-info`/`ModuleMetadata` для каждого модуля
- Create: `src/test/kotlin/org/maxizenit/maxigram/ModularityTest.kt`

- [ ] объявить модули `common/identity/profile/feed/chat/notification/matching/wellbeing`
- [ ] тест `ApplicationModules.verify()` — границы не нарушены
- [ ] прогнать тесты — должны проходить до Этапа 1

### Этап 1 — Identity & Auth (Spring Authorization Server)

#### Task 1.1: Хранилище пользователей и регистрация
**Files:**
- Create: `db/migration/V2__identity.sql` (`app_user`, токены верификации/сброса)
- Create: `identity/UserAccountService.kt`, jOOQ-репозиторий, `identity/web/RegistrationController.kt`
- Create: тесты `UserAccountServiceTest.kt`, `RegistrationControllerIT.kt`

- [ ] миграция `app_user` + токены; пароли через Argon2
- [ ] регистрация пользователя, проверка уникальности email
- [ ] тесты: успешная регистрация, дубликат email, слабый пароль
- [ ] прогнать тесты — должны проходить до Task 1.2

#### Task 1.2: Authorization Server и выдача токенов
**Files:**
- Create: `db/migration/V3__oauth.sql` (таблицы Spring Authorization Server)
- Create: `identity/AuthorizationServerConfig.kt`, `identity/ResourceServerConfig.kt`, `identity/CurrentUser.kt`
- Create: тесты `AuthFlowIT.kt`, `CurrentUserTest.kt`

- [ ] настроить Authorization Server (OIDC, Auth Code + PKCE), reg. client для SPA
- [ ] настроить Resource Server для остальных эндпоинтов; `sub` = `app_user.id`
- [ ] тесты: получение токена, защищённый эндпоинт без токена → 401, чужой id в запросе игнорируется
- [ ] прогнать тесты — должны проходить до Task 1.3

#### Task 1.3: Email-верификация и сброс пароля
**Files:**
- Create: `identity/EmailVerificationService.kt`, `identity/PasswordResetService.kt`
- Create: `common/mail/EmailSender.kt` (базовая отправка, переиспользуется `notification`)
- Create: тесты `EmailVerificationServiceTest.kt`, `PasswordResetServiceTest.kt`

- [ ] флоу верификации email по токену; флоу сброса пароля
- [ ] базовый `EmailSender` (Spring Mail; в тестах — фейковый транспорт)
- [ ] тесты: валидный/просроченный/повторный токен (success + ошибки)
- [ ] прогнать тесты — должны проходить до Этапа 2

### Этап 2 — Профили, интересы, подписки

#### Task 2.1: Профиль и интересы
**Files:**
- Create: `db/migration/V4__profile.sql` (`user_profile`, `interest`, `user_interest`)
- Create: `profile/ProfileService.kt`, jOOQ-репозиторий, `profile/web/ProfileController.kt`
- Create: тесты `ProfileServiceTest.kt`, `ProfileControllerIT.kt`

- [ ] миграции профиля/интересов (+ сид интересов, как в v1)
- [ ] CRUD профиля через jOOQ; владелец — из `CurrentUser`
- [ ] REST: свой профиль (чтение/создание/обновление), список интересов
- [ ] тесты: создание/обновление, нельзя править чужой профиль (success + ошибки)
- [ ] прогнать тесты — должны проходить до Task 2.2

#### Task 2.2: Подписки
**Files:**
- Create: `db/migration/V5__subscription.sql`
- Create: `profile/SubscriptionService.kt`, jOOQ-репозиторий, эндпоинты
- Create: тесты `SubscriptionServiceTest.kt`, `SubscriptionControllerIT.kt`

- [ ] таблица `subscription`, явные INSERT/DELETE через jOOQ
- [ ] подписаться/отписаться/список подписок (исправление #4: подписка реально сохраняется)
- [ ] событие «новый подписчик» (для `notification`)
- [ ] тесты: подписка персистится, повторная идемпотентна, отписка удаляет
- [ ] прогнать тесты — должны проходить до Этапа 3

### Этап 3 — Лента (посты, комментарии, лайки)

#### Task 3.1: Посты и комментарии
**Files:**
- Create: `db/migration/V6__feed.sql` (`post`, `comment`)
- Create: `feed/PostService.kt`, `feed/CommentService.kt`, jOOQ-репозитории, контроллеры
- Create: тесты сервисов и контроллеров

- [ ] миграции `post`/`comment`; автор — из `CurrentUser`
- [ ] создание поста/комментария, чтение, лента подписок по `created_at DESC`
- [ ] события «новый комментарий» (для `notification`)
- [ ] тесты: создание, лента из подписок, пустая лента
- [ ] прогнать тесты — должны проходить до Task 3.2

#### Task 3.2: Лайки и счётчики
**Files:**
- Create: `db/migration/V7__likes.sql` (`post_like`, `comment_like`)
- Create: `feed/LikeService.kt`, агрегатные jOOQ-запросы
- Create: тесты `LikeServiceTest.kt`, `FeedAggregatesIT.kt`

- [ ] раздельные таблицы лайков; like/unlike через явные INSERT/DELETE (исправление #4)
- [ ] `likesCount`/`commentsCount`/`isLikedByCurrentUser` агрегатами без N+1 (исправление #9)
- [ ] событие «лайк» (для `notification`)
- [ ] тесты: лайк персистится и снимается, счётчики корректны, без дублей
- [ ] интеграционный тест: лента из N постов — фиксированное число запросов (нет N+1)
- [ ] прогнать тесты — должны проходить до Этапа 4

### Этап 4 — Чаты и real-time

#### Task 4.1: Обычные чаты и сообщения
**Files:**
- Create: `db/migration/V8__chat.sql` (`chat`, `message`)
- Create: `chat/ChatService.kt`, `chat/MessageService.kt`, jOOQ-репозитории, контроллеры
- Create: тесты сервисов и контроллеров

- [ ] миграции `chat`/`message`; участники из `CurrentUser`
- [ ] создать/получить чат, отправить/прочитать сообщения; «последнее сообщение» по `created_at DESC` (#7)
- [ ] авторизация: доступ к чату только участникам (исправление #8 — IDOR)
- [ ] тесты: отправка/чтение, не-участник → 403, последнее сообщение корректно
- [ ] прогнать тесты — должны проходить до Task 4.2

#### Task 4.2: WebSocket/STOMP — доставка в реальном времени
**Files:**
- Create: `chat/ws/WebSocketConfig.kt`, `chat/ws/ChatSocketController.kt`
- Create: тесты `ChatWebSocketIT.kt`

- [ ] STOMP-эндпоинт, аутентификация по JWT при handshake
- [ ] публикация нового сообщения подписчикам `/topic/chats/{chatId}`
- [ ] событие «новое сообщение» (для `notification`)
- [ ] интеграционный тест: участник получает сообщение по сокету; посторонний не подписывается
- [ ] прогнать тесты — должны проходить до Этапа 5

### Этап 5 — Уведомления

#### Task 5.1: Модуль notification (in-app + email)
**Files:**
- Create: `db/migration/V9__notification.sql` (`notification`)
- Create: `notification/NotificationService.kt`, слушатели событий, STOMP-доставка
- Create: тесты `NotificationServiceTest.kt`, `NotificationDeliveryIT.kt`

- [ ] `@ApplicationModuleListener` на события (новое сообщение, подписчик, лайк/комментарий, подбор)
- [ ] in-app доставка через `/user/queue/notifications`; email через `EmailSender`
- [ ] REST: список уведомлений, пометка прочитанным
- [ ] тесты: событие → создаётся уведомление и доставляется; пометка прочитанным
- [ ] прогнать тесты — должны проходить до Этапа 6

### Этап 6 — Анонимные чаты и деанонимизация

#### Task 6.1: Анонимные чаты — маскировка участников
**Files:**
- Modify: `chat/ChatService.kt` (флаги анонимности; миграция при необходимости)
- Create: тесты `AnonymousChatTest.kt`

- [ ] логика анонимного чата: чужой `participantId` не раскрывается (исправление бессмысленного условия из v1)
- [ ] тесты: участник видит только себя; id собеседника скрыт
- [ ] прогнать тесты — должны проходить до Task 6.2

#### Task 6.2: Взаимная деанонимизация
**Files:**
- Modify: `chat/ChatService.kt` (`agreeToDeAnonymization`)
- Create: тесты `DeAnonymizationTest.kt`

- [ ] согласие выставляет флаг **своей** стороны в `true` (исправление #3, обе стороны симметричны)
- [ ] при обоюдном согласии — найти/создать обычный чат, записать `new_chat_id = newChat.id`
- [ ] тесты: согласие первого, согласие второго, обоюдное → создаётся чат и ссылка; идемпотентность; нельзя в закрытом/неанонимном
- [ ] прогнать тесты — должны проходить до Этапа 7

### Этап 7 — Подбор по похожести

#### Task 7.1: Очередь подбора в БД + доменные события
**Files:**
- Create: `db/migration/V10__matching.sql` (`match_request`, `similarity_calc`)
- Create: `matching/MatchingService.kt`, события `matching/events/*.kt`
- Create: тесты `MatchingServiceTest.kt`

- [ ] очередь кандидатов и состояние расчётов в БД (исправление #6 — без in-memory/race)
- [ ] публикация события на сбор активности (вместо RabbitMQ)
- [ ] тесты: постановка в очередь идемпотентна; параллельные запросы не ломают состояние
- [ ] прогнать тесты — должны проходить до Task 7.2

#### Task 7.2: Формулы похожести
**Files:**
- Create: `matching/SimilarityCalculator.kt`
- Create: тесты `SimilarityCalculatorTest.kt`

- [ ] возраст через `LocalDate`/`Period` (исправление #11)
- [ ] похожесть интересов — симметричная формула с защитой от деления на ноль (исправление #5, без `Infinity`)
- [ ] похожесть активности (общие лайки/комментарии) — корректные коэффициенты
- [ ] тесты: граничные случаи (нет общих интересов, пустые множества, идентичные профили)
- [ ] прогнать тесты — должны проходить до Task 7.3

#### Task 7.3: Сбор активности из feed по событию + создание анонимного чата
**Files:**
- Create: `feed/UserActivityProvider.kt` (слушатель события, агрегатный jOOQ-запрос)
- Modify: `chat`, `notification` (реакция на результат подбора)
- Create: тесты `MatchingFlowIT.kt`

- [ ] `@ApplicationModuleListener` в `feed`: вернуть активность пользователя
- [ ] `matching` агрегирует, при превышении порога публикует «создать анонимный чат»
- [ ] `chat` создаёт анонимный чат, `notification` уведомляет участников
- [ ] интеграционный тест полного потока: запрос → подбор → анонимный чат → уведомление
- [ ] прогнать тесты — должны проходить до Этапа 8

### Этап 8 — Самоограничения (wellbeing)

#### Task 8.1: Самоограничения по времени
**Files:**
- Create: `db/migration/V11__wellbeing.sql` (`self_restraint` со `start_time`/`end_time`)
- Create: `wellbeing/SelfRestraintService.kt`, контроллер, перехватчик доступа
- Create: тесты `SelfRestraintServiceTest.kt`, `SelfRestraintEnforcementIT.kt`

- [ ] таблица `self_restraint` с полями времени (в v1 их не было — дефект #2)
- [ ] установить/получить/снять ограничение; реальная реализация (в v1 — заглушки `super.*`)
- [ ] применение: в заданном интервале доступ к ленте/чатам блокируется
- [ ] тесты: установка/снятие; внутри интервала запрещено, вне — разрешено
- [ ] прогнать тесты — должны проходить до Этапа 9

### Этап 9 — Фронтенд (React SPA)

> Ведётся по готовности API; может идти параллельно этапам 2–8.

#### Task 9.1: Каркас SPA и аутентификация
**Files:**
- Create: `frontend/` (Vite + React + TypeScript), OIDC-клиент (Auth Code + PKCE)
- Create: e2e `frontend/e2e/auth.spec.ts`

- [ ] инициализировать SPA, OIDC-логин к Authorization Server, хранение/обновление токена
- [ ] REST-клиент с прокидыванием `Authorization: Bearer`
- [ ] e2e: регистрация/логин/логаут
- [ ] прогнать e2e — должны проходить до Task 9.2

#### Task 9.2: Лента, профиль, чаты (real-time), уведомления
**Files:**
- Create: экраны `feed`, `profile`, `chats`, центр уведомлений + STOMP-клиент
- Create: e2e `frontend/e2e/{feed,chat}.spec.ts`

- [ ] лента (посты/лайки/комментарии), профиль/подписки
- [ ] чаты с real-time через STOMP; корректные состояния кнопок (исправление инверсии #10)
- [ ] экран анонимного чата с согласием на деанонимизацию; центр уведомлений (push)
- [ ] e2e: сообщение видно второму участнику; согласие на деанонимизацию; приходит уведомление
- [ ] прогнать e2e — должны проходить до финала

### Финал

#### Task F.1: Проверка критериев приёмки
- [ ] все пункты из Overview реализованы; карта дефектов v1 закрыта
- [ ] прогнать полный набор: `./gradlew test` (вкл. `ApplicationModules.verify()` и Testcontainers)
- [ ] прогнать e2e фронта
- [ ] проверить отсутствие N+1 на ленте и в потоке подбора

#### Task F.2: Документация
- [ ] `README.md`: запуск (docker compose), профили, переменные окружения
- [ ] `CLAUDE.md`: конвенции (Kotlin, jOOQ, границы модулей, identity из JWT)
- [ ] перенести этот план в `docs/plans/completed/`

## Post-Completion
*Внешние действия, без чекбоксов — справочно.*

**Ручная проверка:**
- сценарии UX чатов и анонимности на реальных пользователях;
- нагрузочное тестирование ленты и подбора;
- ревью безопасности (валидация ввода, rate limiting, доступ к WebSocket, политика паролей).

**Инфраструктура/деплой:**
- актуальные TLS-протоколы в nginx (убрать TLSv1/1.1), HTTPS для всех URL;
- секреты через переменные окружения/secret-manager, не в VCS;
- SMTP-провайдер для email (верификация/сброс/уведомления);
- docker compose v2: один сервис приложения + PostgreSQL, без RabbitMQ, gRPC и Keycloak.
