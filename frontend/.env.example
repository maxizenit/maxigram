# Copy to .env.local for local dev. The backend (incl. the OIDC Authorization Server) defaults to :8080.
VITE_API_URL=http://localhost:8080
VITE_OIDC_AUTHORITY=http://localhost:8080
VITE_OIDC_CLIENT_ID=maxigram-spa
